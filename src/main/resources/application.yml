server:
  port: 8095
  error:
    include-message: always

spring:
  main:
    web-application-type: servlet
    allow-circular-references: true


  datasource:
    url: jdbc:postgresql://localhost:5432/tours
    username: postgres
    password: h01112002

  jpa:
    hibernate:
          ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true